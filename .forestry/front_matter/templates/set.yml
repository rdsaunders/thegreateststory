---
label: Timeline
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: period
  type: text
  config:
    required: false
  label: Period
- name: excerpt
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Summary
- name: order
  type: number
  label: Display order
  description: ''
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
- name: set_download
  type: file
  config:
    maxSize: 250
  label: Set download
